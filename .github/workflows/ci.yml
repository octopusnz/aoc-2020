name: Build C Project
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  headergrab:
    if: ${{ github.event_name == 'push' && github.repository_owner == 'octopusnz' }}
    permissions:
      contents: write
      pull-requests: write
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup MSBuild (Windows)
        if: runner.os == 'Windows'
        uses: microsoft/setup-msbuild@v2

      - name: Detect and PR SDK header changes
        if: runner.os == 'Windows'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: pwsh
        run: |
          # Define source and destination files
          $targetFiles = @(
            "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\SDK\ScopeCppSDK\vc15\SDK\include\ucrt\io.h",
            "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\SDK\ScopeCppSDK\vc15\SDK\include\ucrt\corecrt_io.h",
            "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\SDK\ScopeCppSDK\vc15\SDK\include\ucrt\corecrt_share.h",
            "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\SDK\ScopeCppSDK\vc15\SDK\include\ucrt\corecrt_wio.h"
          )
          
          # Use the checked-out workspace for destination
          $repoDest = Join-Path $env:GITHUB_WORKSPACE 'docs'
          $changedFiles = @{}
          $timestamp = Get-Date -Format "yyyyMMdd-HHmmss"
          $branchName = "io-sync-$timestamp"
          $baseBranch = '${{ github.event.repository.default_branch }}'
      
          # Step 1: Detect changes
          foreach ($src in $targetFiles) {
            $fileName = Split-Path $src -Leaf
            $dest = Join-Path $repoDest $fileName
            
            if (!(Test-Path $src)) {
              Write-Host "⚠️ Source not found, skipping: $src"
              continue
            }

            if (!(Test-Path $dest) -or (Get-FileHash $src).Hash -ne (Get-FileHash $dest).Hash) {
              $changedFiles[$fileName] = @{ Src = $src; Dest = $dest }
            }
          }
      
          if ($changedFiles.Count -eq 0) {
            Write-Host "🚫 No file changes detected. Skipping PR."
            exit 0
          }
      
          # Step 2: Detect blocked files in open PRs
          $prList = gh pr list --state open --json number | ConvertFrom-Json
          $blockedSet = New-Object System.Collections.Generic.HashSet[string]
            
          foreach ($pr in $prList) {
            Write-Host "`n🔄 Fetching file list for PR #$($pr.number)..."
            $files = gh pr view $pr.number --json files --jq '.files[].path' 2>$null
            if (-not $files -or $files.Count -eq 0) {
              Write-Host "⚠️ Skipping PR #$($pr.number): no file data returned."
              continue
            }
            Write-Host "`n🔍 PR #$($pr.number) Files:"
            foreach ($path in $files) {
              $base = Split-Path $path -Leaf
              $normalized = $base.ToLower().Trim()
              Write-Host "📄 $normalized"
              $null = $blockedSet.Add($normalized)
            }
          }
            
          Write-Host "`n📁 Blocked Set Contents (so far):"
          $blockedSet | Sort-Object | ForEach-Object { Write-Host "🔒 $_" }
          
          Write-Host "`n🔍 Debugging File Inclusion Logic:"
          $changedFiles.Keys | ForEach-Object {
            $normalized = $_.ToLower().Trim()
            $status = if ($blockedSet.Contains($normalized)) { "❌ Blocked" } else { "✅ Included" }
            Write-Host "$_ → $status"
          }

          $includedFiles = $changedFiles.Keys | Where-Object { -not $blockedSet.Contains($_.ToLower().Trim()) }
      
          # Step 3: Diagnostics
          Write-Host "`n🧾 Changed Files: $($changedFiles.Keys -join ', ')"
          Write-Host "🚫 Blocked Files: $([string]::Join(', ', $blockedSet))"
          Write-Host "✅ Included Files for PR: $($includedFiles -join ', ')`n"
      
          if ($includedFiles.Count -eq 0) {
            Write-Host "✅ All changed files are already covered in open PRs. No new PR created."
            exit 0
          }
      
          # Only create branch and PR if there are included files
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git fetch origin --no-tags
          git checkout -B $branchName origin/$baseBranch

          foreach ($file in $includedFiles) {
            Copy-Item -Path $changedFiles[$file].Src -Destination $changedFiles[$file].Dest -Force
            git add $changedFiles[$file].Dest
          }

          $commitMsg = "Header sync: $($includedFiles -join ', ')"
          git commit -m $commitMsg
          git push -u origin $branchName --force

          $bodyText = @"
          🔧 Automated header sync detected the following updates from Visual Studio SDK.

          ✅ Included files: $($includedFiles -join ', ')
          "@

          if ($blockedSet.Count -gt 0) {
            $bodyText = @"
            ❌ Files already handled by other PRs:
             
            "$([string]::Join("`n", $blockedSet))"
          "@
          }
      
          gh pr create `
            --base $baseBranch `
            --head $branchName `
            --title $commitMsg `
            --body $bodyText

  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup MSBuild (Windows)
        if: runner.os == 'Windows'
        uses: microsoft/setup-msbuild@v2

      - name: Setup MSVC (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Compile (Linux)
        if: runner.os == 'Linux'
        run: |
          gcc -Wall -Wextra -std=c89 -Ilibs 01/main.c libs/*.c -o main01
          gcc -Wall -Wextra -std=c89 -Ilibs 02/main.c libs/*.c -o main02

      - name: Compile (macOS)
        if: runner.os == 'macOS'
        run: |
          clang -Wall -Wextra -std=c89 -Ilibs 01/main.c libs/*.c -o main01
          clang -Wall -Wextra -std=c89 -Ilibs 02/main.c libs/*.c -o main02

      - name: Compile (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          cl /W4 /TC /Ilibs 01\main.c libs\*.c /Fe:main01.exe
          cl /W4 /TC /Ilibs 02\main.c libs\*.c /Fe:main02.exe

  tests:
    name: Build and run Unity tests
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build and run tests (01)
        run: |
          make -C 01 unity
          ./01/unity.out

      - name: Build and run tests (02)
        run: |
          make -C 02 unity
          ./02/unity.out

  coverage:
    name: Code coverage (gcovr)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install gcovr
        run: |
          sudo apt-get update
          sudo apt-get install -y gcovr
      - name: Build coverage binaries and run tests
        run: |
          make -C 01 unity-coverage
          ./01/unity_coverage.out
          make -C 02 unity-coverage
          ./02/unity_coverage.out
      - name: Generate HTML report
        run: |
          gcovr -r . --html --html-details -o coverage.html \
            --exclude-directories=.github \
            --exclude=libs/unity/.* \
            --exclude=tests/.* \
            --exclude=docs/.*
      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.html
